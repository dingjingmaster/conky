NAME
       conky  - A system monitor for X originally based on the torsmo code, but more kickass.
       It just keeps on given'er. Yeah.

To use with dwm configure with:

     ./configure --disable-x11 --disable-lua --no-create --no-recursion

If the configure script does not complete also run ./config.status
